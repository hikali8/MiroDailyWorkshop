<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.MiroDailyUI" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- 背景与系统栏（浅色） -->
        <item name="android:windowBackground">@color/bg_page</item>
        <item name="android:navigationBarColor">@color/bg_page</item>
        <item name="android:statusBarColor">@color/topbar_bg</item>

        <!-- 关键：禁用系统强制深色（仅保留 android: 前缀的这一条） -->
        <item name="android:forceDarkAllowed">false</item>
    </style>
</resources>
